export const designDocContent = `# 마법기사 키우기 - 장비 슬롯 강화 시스템 기획서

> **작성자**: 시스템 기획
> **버전**: v0.5
> **범위**: 시스템 구조 및 테이블 설계 (밸런스 수치 제외)

---

## 1. 시스템 개요

### 1.1 핵심 컨셉

> **"장비가 아닌 슬롯을 강화한다"**

- 장비 부위(슬롯) 자체에 강화 레벨이 존재한다
- 슬롯 강화는 해당 슬롯에 장착된 장비의 **기본 스탯을 증폭**시킨다
- 장비를 교체해도 슬롯의 강화 상태는 **유지**된다
- 더 좋은 장비를 얻으면 교체만 하면 슬롯 강화 효과가 자동 적용된다

---

## 2. 장비 부위 정의

> 현재 장비 부위는 **임시로 10개**로 지정한다.
> 부위 수 및 구성은 추후 기획 확정에 따라 변경될 수 있으며,
> 테이블에 행을 추가/삭제하는 것만으로 부위 수를 조정할 수 있도록 설계한다.

| 슬롯 ID | 부위명 |
|---------|--------|
| SLOT_WEAPON | 지팡이 |
| SLOT_HAT | 모자 |
| SLOT_ROBE | 로브(상의) |
| SLOT_BOTTOM | 하의 |
| SLOT_GLOVE | 장갑 |
| SLOT_SHOE | 신발 |
| SLOT_CLOAK | 망토 |
| SLOT_RING | 반지 |
| SLOT_NECKLACE | 목걸이 |
| SLOT_EARRING | 귀걸이 |

---

## 3. 테이블 구조

> 테이블 구조를 먼저 정의하고, 이후 섹션에서 각 테이블의 동작 방식을 설명한다.

### 3.1 T_EQUIP_SLOT (장비 슬롯 정의 테이블)

장비 슬롯의 종류를 정의하는 테이블. 행을 추가하면 슬롯이 늘어나고, 삭제하면 줄어든다.

| 컬럼명 | 설명 |
|--------|------|
| slotId | 슬롯 고유 식별자. 예: "SLOT_WEAPON", "SLOT_HAT" |
| slotName | 슬롯 표시 이름. 예: "지팡이", "모자" |
| slotOrder | UI에서의 표시 순서. 숫자가 작을수록 먼저 표시 |
| maxEnhanceLevel | 해당 슬롯의 최대 강화 레벨. 슬롯마다 다르게 설정 가능 |

### 3.2 T_SLOT_ENHANCE (슬롯 강화 레벨 테이블)

슬롯의 강화 레벨별 동작을 정의하는 **핵심 테이블**. 기획자가 이 테이블의 값을 조정하여 강화 시스템의 모든 동작을 제어한다.

| 컬럼명 | 설명 |
|--------|------|
| slotId | 대상 슬롯 ID. "ALL"이면 공통 적용 |
| level | 강화 레벨. 0부터 maxEnhanceLevel까지 |
| statRate | 이 레벨에서의 스탯 증폭 배율 (누적값) |
| successRate | 이 레벨로 강화할 때의 성공 확률 (0.0~1.0) |
| failBonusRate | 연속 실패 시 성공률 누적 가산값 |
| failType | 실패 시 동작: KEEP / DOWN_1 / RESET |
| isSafe | 안전 강화 여부 (true면 100% 성공) |
| costType1/Amount1 | 소모 재화 1 |
| costType2/Amount2 | 소모 재화 2 (선택) |

### 3.3 T_SLOT_MILESTONE (슬롯 마일스톤 테이블)

개별 슬롯이 특정 강화 레벨에 도달했을 때의 추가 보상/연출을 정의하는 테이블.

| 컬럼명 | 설명 |
|--------|------|
| slotId | 대상 슬롯 ID. 공통 적용이면 "ALL" |
| level | 마일스톤이 발동되는 강화 레벨 |
| starDisplay | 이 레벨에서 표시할 별 개수 |
| effectId | 시각 이펙트 ID |
| rewardType | 마일스톤 보상 유형 |
| rewardValue | 보상 값 |

### 3.4 T_SLOT_TOTAL_MILESTONE (전체 합산 마일스톤 테이블)

모든 슬롯의 강화 레벨을 합산한 값이 특정 수치에 도달했을 때의 추가 보너스를 정의.

| 컬럼명 | 설명 |
|--------|------|
| totalLevel | 전체 합산 강화 레벨 |
| bonusStatType | 보너스 스탯 종류 |
| bonusStatValue | 보너스 수치 |
| bonusDesc | UI 표시 설명 |
| titleReward | 달성 칭호 |

### 3.5 T_MILESTONE_STAT_POOL (마일스톤 스텟 풀 테이블)

마일스톤 도달 시 획득할 수 있는 스텟의 종류와 등급별 수치를 정의.

| 컬럼명 | 설명 |
|--------|------|
| milestoneLevel | 이 스텟이 등장하는 마일스톤 레벨 |
| statType | 스텟 종류 식별자 |
| grade | 스텟 등급 (NORMAL~ANCIENT) |
| statValue | 이 등급에서의 스텟 수치 |
| weight | statType 선택 확률 가중치 |

> **조회 키**: (milestoneLevel, statType, grade)로 유일한 행이 결정된다.

### 3.6 T_MILESTONE_GRADE_RATE (마일스톤 등급 확률 테이블)

슬롯 강화 레벨에 따라 마일스톤 스텟의 등급 등장 확률을 정의.

| 컬럼명 | 설명 |
|--------|------|
| enhanceLevel | 슬롯 강화 레벨 |
| grade | 스텟 등급 |
| rate | 해당 등급의 등장 확률 (합계 = 1.0) |

### 3.7 T_MILESTONE_SLOT (마일스톤 슬롯 테이블)

마일스톤 슬롯 해금 조건과 스텟 수를 정의.

| 컬럼명 | 설명 |
|--------|------|
| slotId | 대상 슬롯 ID. "ALL"이면 공통 적용 |
| unlockLevel | 마일스톤 슬롯이 열리는 강화 레벨 |
| statCount | 이 마일스톤 슬롯에 포함되는 스텟 개수 |

> **설계 원칙**: 마일스톤 슬롯이 열리면 statCount 개의 스텟이 한꺼번에 롤링된다.
> 레벨이 높아질수록 슬롯당 스텟 수가 점진적으로 증가하여 성장감을 극대화한다.

### 3.8 T_MILESTONE_REROLL (마일스톤 스텟 리롤 테이블)

리롤 시 잠금(Lock) 기능과 비용을 정의.

| 컬럼명 | 설명 |
|--------|------|
| milestoneLevel | 대상 마일스톤. "ALL"이면 공통 적용 |
| rerollCostType1/Amount1 | 리롤 기본 비용 재화 1 |
| rerollCostType2/Amount2 | 리롤 기본 비용 재화 2 (선택) |
| lockCostType1/Amount1 | 잠금 1개당 추가 비용 재화 1 |
| lockCostType2/Amount2 | 잠금 1개당 추가 비용 재화 2 (선택) |

---

## 4. 슬롯 강화 메커니즘

### 4.1 강화 레벨

- 모든 슬롯은 강화 레벨 **0** 에서 시작한다
- 강화 1회 시도마다 성공/실패 판정이 이루어진다
- 최대 레벨은 T_EQUIP_SLOT 테이블의 **maxEnhanceLevel** 값으로 결정된다
- 최대 레벨에 도달한 슬롯은 더 이상 강화할 수 없다

### 4.2 강화 재료

- 강화 시도 시 T_SLOT_ENHANCE 테이블에 정의된 재화를 소모한다
- 재화 종류와 수량은 레벨마다 다르게 설정 가능
- 재화가 부족하면 강화를 시도할 수 없다
- 재화는 **시도 시 소모**된다 (성공/실패 관계없이)

### 4.3 강화 판정

#### 4.3.1 성공 판정 흐름

\`\`\`
1. T_SLOT_ENHANCE에서 현재 레벨+1에 해당하는 행을 조회
2. isSafe == true 이면 → 무조건 성공
3. isSafe == false 이면:
   실제 성공률 = successRate + (연속 실패 횟수 × failBonusRate)
   실제 성공률이 1.0을 초과하면 1.0으로 제한
   난수(0.0~1.0) < 실제 성공률 이면 → 성공
\`\`\`

#### 4.3.2 성공 시

- 슬롯 강화 레벨이 +1 상승한다
- 연속 실패 횟수가 0으로 초기화된다
- 해당 레벨에 마일스톤이 있으면 마일스톤 보상이 지급된다

#### 4.3.3 실패 시

- T_SLOT_ENHANCE 테이블의 **failType** 값에 따라 동작한다:

| failType | 동작 |
|----------|------|
| KEEP | 현재 레벨 유지. 아무 패널티 없음 |
| DOWN_1 | 강화 레벨이 1 하락 (최소 0) |
| RESET | 강화 레벨이 0으로 초기화 |

- 연속 실패 횟수가 +1 증가한다

### 4.4 연속 실패 보정

- 동일 레벨에서 실패할 때마다 **failBonusRate** 만큼 성공률이 누적 가산된다
- 성공하면 연속 실패 횟수가 0으로 초기화된다
- failBonusRate를 0으로 설정하면 보정 기능을 끌 수 있다

### 4.5 안전 강화

- **isSafe = true** 로 설정된 레벨은 무조건 성공한다
- 안전 강화 시에도 재료는 정상 소모된다

---

## 5. 스탯 적용

### 5.1 증폭 공식

\`\`\`
슬롯 최종 스탯 = 장비 기본 스탯 × (1 + statRate)
\`\`\`

- **statRate**는 T_SLOT_ENHANCE에서 현재 레벨의 값
- 장비의 **기본 스탯에만 적용** (서브 옵션, 세트 효과 등에는 미적용)

### 5.2 장비 교체 시 동작

- 장비를 교체하면 **새 장비의 기본 스탯**에 기존 슬롯 강화 배율이 즉시 적용된다
- 슬롯의 강화 레벨, 연속 실패 횟수 등은 일절 변경되지 않는다
- 장비가 미장착된 슬롯도 강화가 가능하다

---

## 6. 마일스톤 시스템

### 6.1 개별 슬롯 마일스톤 (별 표시)

- T_SLOT_MILESTONE 테이블에 정의된 레벨에 도달하면 발동
- 별 표시 변경, 시각 이펙트 적용 등 **시각적 연출** 담당
- Lv.5/10/15/20/25/30 에서 각각 1~6성 표시

### 6.2 마일스톤 슬롯 시스템

개별 슬롯이 특정 강화 레벨에 도달하면 **마일스톤 슬롯**이 열리며, 각 슬롯에는 여러 개의 마일스톤 스텟이 포함된다.

#### 마일스톤 슬롯 구성

| 해금 레벨 | 스텟 수 | 누적 스텟 수 |
|----------|---------|-------------|
| Lv.5 | 3개 | 3 |
| Lv.10 | 4개 | 7 |
| Lv.15 | 5개 | 12 |
| Lv.20 | 6개 | 18 |
| Lv.25 | 7개 | 25 |
| Lv.30 | 8개 | 33 |

#### 마일스톤 슬롯 해금 흐름

\`\`\`
1. 슬롯이 특정 레벨에 도달 (예: Lv.5)
2. T_MILESTONE_SLOT에서 해당 unlockLevel에 해당하는 행 조회
3. statCount 개의 스텟을 한꺼번에 롤링
4. 각 스텟은 T_MILESTONE_STAT_POOL + T_MILESTONE_GRADE_RATE 기반으로 결정
5. 마일스톤 슬롯이 열리면 이후 강화 레벨이 떨어져도 슬롯은 유지됨
\`\`\`

#### 설계 포인트

- **단계적 성장감**: 레벨이 올라갈수록 더 많은 스텟을 획득
- **슬롯별 독립 관리**: 각 마일스톤 슬롯은 독립적으로 리롤/잠금 가능
- **장비 1개당 최대 33개 스텟**: 10개 장비 × 33 = 최대 330개 마일스톤 스텟

### 6.3 마일스톤 스텟 등급 시스템

마일스톤 슬롯에서 부여되는 스텟에 **등급(Grade)** 개념이 적용된다.

#### 등급 종류

| 등급 | 설명 |
|------|------|
| NORMAL | 기본 등급. 저레벨에서 가장 높은 확률로 등장 |
| RARE | 약간 상승된 수치. 중저레벨부터 등장 |
| EPIC | 높은 수치. 중레벨부터 의미 있는 확률로 등장 |
| LEGENDARY | 매우 높은 수치. 고레벨에서 주로 등장 |
| ANCIENT | 최상위 등급. 최고레벨에서만 유의미한 확률로 등장 |

#### 스텟 결정 흐름

\`\`\`
1. T_MILESTONE_STAT_POOL에서 해당 milestoneLevel의 statType 목록 조회
2. weight 기반 가중치 랜덤으로 statType 1개 선택
3. T_MILESTONE_GRADE_RATE에서 현재 enhanceLevel의 등급별 확률 조회
4. rate 기반 가중치 랜덤으로 grade 1개 결정
5. T_MILESTONE_STAT_POOL에서 (milestoneLevel, statType, grade) 조합의 statValue 적용
\`\`\`

> 마일스톤 슬롯의 statCount 만큼 위 과정을 반복한다. 동일 스텟 종류가 중복 등장할 수 있다.

### 6.4 마일스톤 스텟 리롤

마일스톤 슬롯의 스텟이 마음에 들지 않을 경우 **리롤**하여 새로운 스텟을 받을 수 있다.

#### 리롤 동작

- 리롤은 **마일스톤 슬롯 단위**로 실행된다
- 잠금되지 않은 스텟만 재추첨한다
- 스텟 풀은 해당 마일스톤 슬롯의 **unlockLevel** 기준
- 등급 확률은 **현재 슬롯 강화 레벨**의 가장 가까운 하위 레벨 기준

#### 잠금(Lock) 기능

- 보존하고 싶은 스텟에 잠금을 걸 수 있다
- 잠금된 스텟은 리롤 시 변경되지 않는다
- 잠금 1개당 추가 비용 발생
- 모든 스텟을 잠그고 리롤하는 것은 불가능 (최소 1개 잠금 해제)

#### 비용 계산

\`\`\`
총 리롤 비용 = 기본 리롤 비용 + (잠금 스텟 수 × 잠금 1개당 비용)
\`\`\`

### 6.5 전체 합산 마일스톤

- 모든 슬롯의 현재 강화 레벨을 합산한 값을 기준으로 판정
- T_SLOT_TOTAL_MILESTONE에 정의된 합산 레벨에 도달하면 보너스 스탯 적용
- 보너스 스탯은 장비 스탯과 별개로 캐릭터에 직접 적용
- 특정 합산 레벨에서 칭호를 부여할 수 있다

---

## 7. 연속 강화

### 7.1 10연속 강화 기능

- "10연속 강화" 버튼으로 최대 10회까지 자동 강화 시도
- 10회 완료, 최대 레벨 도달, 또는 재료 부족 시 중단
- 완료 후 결과 요약 표시 (시도 횟수, 성공/실패 횟수, 최종 레벨)

---

> **문서 끝** - 밸런스 수치는 별도 밸런스 시트에서 관리하며 확정 후 T_SLOT_ENHANCE 테이블에 반영한다.
`;
